name: 'Mem Info'
description: 'Memory Inforation Action'
inputs:
  myInput:
    description: 'greeting to use' 
    required: true
    default: 'Awesome Guru' 
outputs:
  myOutput:
    description: 'total memory'
runs:
  using: docker
  image: 'Dockerfile'
  
